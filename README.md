# Coordinator Core Releases

This public repository is the distribution channel for verified Coordinator Core WordPress plugin releases.

It contains only release packages, checksums, signatures, and minimal update metadata. Development source, project records, credentials, signing keys, and operational secrets do not belong in this repository.

WordPress must verify the published package checksum and cryptographic signature before offering or installing an update. Updates require explicit administrator approval and use WordPress's native updater and recovery mechanisms.
